#!/usr/bin/env bash
set -Eeuo pipefail

output=
input=
while [[ $# -gt 0 ]]; do
  case $1 in
    --recipient) shift 2 ;;
    --output) output=$2; shift 2 ;;
    *) input=$1; shift ;;
  esac
done
[[ -n ${output} && -n ${input} ]]
cp "${input}" "${output}"
